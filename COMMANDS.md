# Commands

This repository currently has no stack-specific toolchain. Use the generic Git commands below now, and fill in the build tool commands after the technology stack is chosen.

## Current Repository Checks

- `git status --short` - show untracked and modified files in a compact form
- `git diff` - inspect line-by-line changes before review

## After the Stack Is Chosen

Add the concrete commands for the selected toolchain in the sections below.

### Install

Replace this with the project install command for the chosen stack:

```text
<install command>
```

### Start

Replace this with the local start or dev-server command:

```text
<start command>
```

### Test

Replace this with the test command used locally and in CI:

```text
<test command>
```

### Lint

Replace this with the static analysis command:

```text
<lint command>
```

### Format

Replace this with the formatting command:

```text
<format command>
```

### Build

Replace this with the production build command:

```text
<build command>
```

## Maintenance Notes

- Keep the commands in this file aligned with the actual scripts or task runners in the repository.
- Update this file whenever the selected stack or tooling changes.
- Do not add dependency-specific instructions until the stack is finalized.
